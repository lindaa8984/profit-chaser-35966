# دليل التطوير المحلي والنسخ الاحتياطية

## 🔄 النسخ الاحتياطية

### النسخ الاحتياطية الحالية
- **النسخ اليدوية**: يجب عليك إنشاء النسخ الاحتياطية يدويًا من خلال النظام
- **النسخ التلقائية**: قاعدة البيانات السحابية (Lovable Cloud) تحتفظ بنسخ احتياطية يومية تلقائية
- **تتبع النسخ**: يتم الآن تسجيل كل نسخة احتياطية تقوم بإنشائها مع التاريخ والوقت

### كيفية عمل نسخة احتياطية
1. اذهب إلى أي صفحة في النظام
2. اضغط على زر "نسخ احتياطي" 
3. اختر نوع النسخة:
   - **JSON**: نسخة كاملة بتنسيق JSON (يمكن استعادتها بالكامل)
   - **Excel**: نسخة للعرض والتحليل

### توصيات النسخ الاحتياطي
- ✅ قم بعمل نسخة احتياطية أسبوعيًا على الأقل
- ✅ احفظ النسخ في مكان آمن (Google Drive، Dropbox، أو قرص خارجي)
- ✅ احتفظ بنسخ متعددة (3 نسخ على الأقل من أوقات مختلفة)
- ✅ تحقق من تاريخ آخر نسخة احتياطية بانتظام

### الأمان
- قاعدة البيانات السحابية (Lovable Cloud) محمية بنسخ احتياطية يومية تلقائية
- يتم الاحتفاظ بالنسخ لمدة 7 أيام
- في حالة فقدان البيانات، يمكن استعادتها من النسخ التلقائية

---

## 💻 التطوير المحلي

### الخيار 1: التطوير مع الاتصال بقاعدة البيانات السحابية (موصى به)

هذا هو الخيار الأسهل والأكثر موثوقية.

#### المتطلبات
- Node.js (الإصدار 18 أو أحدث)
- npm أو yarn
- Git

#### الخطوات

1. **استنساخ المشروع من GitHub**
   ```bash
   # إذا كان المشروع متصلاً بـ GitHub
   git clone <your-github-repo-url>
   cd <project-folder>
   ```

2. **تثبيت الحزم**
   ```bash
   npm install
   ```

3. **ملف البيئة موجود بالفعل**
   - ملف `.env` يحتوي على جميع إعدادات الاتصال بقاعدة البيانات
   - لا تحتاج لتعديله

4. **تشغيل المشروع محليًا**
   ```bash
   npm run dev
   ```
   سيفتح المشروع على `http://localhost:8080`

5. **التعديل والتطوير**
   - قم بتعديل الملفات في مجلد `src`
   - التغييرات ستظهر فورًا في المتصفح
   - البيانات متصلة بقاعدة البيانات السحابية

6. **رفع التحديثات**
   ```bash
   git add .
   git commit -m "وصف التعديلات"
   git push
   ```

#### المزايا
✅ سهل التنفيذ
✅ البيانات الحقيقية متاحة
✅ لا حاجة لإعداد قاعدة بيانات منفصلة
✅ يمكنك العمل على أي جهاز

#### العيوب
⚠️ يتطلب اتصال بالإنترنت
⚠️ التعديلات على البيانات تؤثر على قاعدة البيانات الحقيقية

---

### الخيار 2: التطوير مع قاعدة بيانات محلية (متقدم)

هذا الخيار للمطورين المحترفين فقط.

#### المتطلبات
- Docker Desktop
- Supabase CLI
- معرفة متقدمة بـ Supabase و PostgreSQL

#### الخطوات

1. **تثبيت Supabase CLI**
   ```bash
   npm install -g supabase
   ```

2. **تثبيت Docker Desktop**
   - قم بتحميل وتثبيت Docker Desktop من: https://www.docker.com/products/docker-desktop

3. **إعداد Supabase المحلي**
   ```bash
   supabase init
   supabase start
   ```

4. **تحديث ملف `.env` للاتصال المحلي**
   ```env
   VITE_SUPABASE_URL=http://localhost:54321
   VITE_SUPABASE_PUBLISHABLE_KEY=<your-local-anon-key>
   ```

5. **تطبيق الهجرات (Migrations)**
   ```bash
   supabase db reset
   ```

6. **تشغيل المشروع**
   ```bash
   npm run dev
   ```

#### المزايا
✅ لا يتطلب اتصال بالإنترنت
✅ بيئة تطوير معزولة تمامًا
✅ يمكنك تجربة التغييرات بحرية

#### العيوب
⚠️ إعداد معقد
⚠️ يستهلك موارد الجهاز
⚠️ يحتاج معرفة تقنية متقدمة
⚠️ البيانات منفصلة عن الإنتاج

---

### الخيار 3: وضع التطوير مع بيانات وهمية (Mock Data)

خيار مناسب للتطوير السريع بدون اتصال.

#### يمكننا إنشاء نظام بيانات وهمية إذا كنت تريد ذلك
- إنشاء ملف `mock-data.ts` يحتوي على بيانات تجريبية
- إضافة خيار في الإعدادات للتبديل بين البيانات الحقيقية والوهمية
- سيسمح لك بالعمل بدون اتصال بالإنترنت

---

## 🤔 أي خيار تختار؟

### للاستخدام اليومي والتطوير العادي
👉 **الخيار 1** (التطوير مع الاتصال بقاعدة البيانات السحابية)

### للتطوير المعقد أو التجارب الكبيرة
👉 **الخيار 2** (قاعدة بيانات محلية مع Docker)

### للعمل بدون إنترنت
👉 **الخيار 3** (بيانات وهمية) - يتطلب تنفيذ إضافي

---

## 📞 الدعم

إذا واجهت أي مشاكل أو كان لديك أسئلة:
1. راجع ملف `README.md` الرئيسي
2. تحقق من وثائق Lovable: https://docs.lovable.dev
3. تحقق من وثائق Supabase: https://supabase.com/docs
